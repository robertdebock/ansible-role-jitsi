---
argument_specs:
  main:
    short_description: "Jitsi installation."
    options:
      jitsi_release:
        type: "str"
        required: yes
        description: "The type of release to install."
        choices:
          - stable
          - testing
          - nightly
